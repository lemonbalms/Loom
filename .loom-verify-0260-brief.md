# [VERIFY-0260] PLAN v0.26.0 독립 락 대조 — hooks 보조 센서 (codex pane · 읽기 전용)

당신은 Loom dogfood 룸의 **독립 검증자(codex)**다.  
**verify-0250 선례 동형** — 구현이 R41 binding lock + L-1..L-3을 코드에 정확히 반영했는지 **문서(PLAN/plan_review) ↔ 코드 대조**만 한다.

## 절대 금지
- 제품 코드·테스트·문서 **수정 금지**
- `git commit` / `git push` **금지**
- 의존성 설치·브릿지 재시작·config 쓰기 금지
- 구현 재작성·리팩터 제안 본문 금지 (PASS/FAIL + 근거 줄 번호만)

## 정본 (이 순서)
1. `docs/PLAN.md` — `#### 0.26.0` 블록 (D1~D7, M-1·M-2 문안, L-1..L-3, 테스트 계획)
2. `docs/plan_review.md` — **R41** Findings M-1·M-2 + L-1..L-3 + author-close 로그
3. 구현 (워크트리 미커밋 포함 — **현재 디스크가 SSOT**):
   - `packages/host/src/hook-sensor.ts`
   - `packages/host/src/hook-sensor.test.ts`
   - `packages/host/src/bridge-runtime.ts` (hook 배선)
   - `packages/host/src/bridge-config.ts` (`hookSensor`)
   - `packages/host/src/index.ts`
   - `packages/cli/src/index.ts` VERSION
   - `packages/mcp-server/src/stdio.ts` VERSION

## 대조 체크리스트 (각 행 PASS/FAIL + 파일:줄 근거)

### M-1 (attempt-scoped 소켓 + 생명주기)
| # | 요구 | 확인 방법 |
|---|------|-----------|
| M-1.1 | 소켓 경로 = `hook-<cardId>-<seq>.sock` (cardId만이 아님) | `hookSocketPath` |
| M-1.2 | seq = herdr agent명 `loom-…-<seq>` 와 동형 (runCard seq) | `bridge-runtime` spawn 직전 |
| M-1.3 | bind 전 기존 경로 unlink | `startHookListener` |
| M-1.4 | flight 소멸 시 close+unlink | `disposeCardFlight` / flight teardown |
| M-1.5 | 늦은 hook 이벤트 = flight 동일성 가드 드롭 | `onEvent` 콜백 `flights.get(paneId) !== f` |

### M-2 (hookHint 소비 계약 4항)
| # | 요구 | 확인 방법 |
|---|------|-----------|
| M-2.1 | 단일 슬롯 last-event-wins (누적 금지) | `flight.hookHint` 타입·대입 |
| M-2.2 | herdr `working` 재전이 시 마커 소거 | `clearHookHintOnWorking` 호출 |
| M-2.3 | `permission_prompt` **최신·미소거일 때만** 완료-클래스 → `agent_blocked` 교정 | `shouldCorrectCompletionToBlocked` + done/idle·indicator-clear·exhaust 경로 |
| M-2.4 | 스테일/소거 마커 → **무교정** (정상 done 보존) | 테스트 + 분기 조건 |
| M-2.5 | `Stop` = poll 가속/상한 우회 **입력만**; indicator 잔존 중 단독 완료 금지 | `stopAllowsStillRunningMaxBypass` + poll 루프 |

### L-1 · L-2 · L-3
| # | 요구 | 확인 방법 |
|---|------|-----------|
| L-1 | cardId sanitize+slice(≤80), **slice는 cardId만** (seq 접미 유일성 보존) | `sanitizeCardIdForSocket` |
| L-2 | 무회귀 스코프 = 판정·wire 동작 (argv hook 주입·계측 append 제외) | fail-open: hookSensor off / bind 실패 시 현행 경로 |
| L-3 | 계측 JSONL = 메타데이터 only (`last_assistant_message`/`prompt`/`tool_input` 미기록) | `appendHookTelemetry` + `HookHint` 타입 |

### D2 · D4 · D5 · D6 · D7 (스모크성)
| # | 요구 |
|---|------|
| D2 | claude + `hookSensor===true` 일 때만 `--settings` 주입; handler `async:true`; exit 0·stdout 비움 규약 |
| D4 | 0600 + `isPathUnderLoomDir` + 비정상 페이로드 조용히 무시(카운터) |
| D5 | hook 부재/bind 실패 → 현행 경로 (fail-open) |
| D6 | `hook-sensor.jsonl` 계측 4종 카운터 계열 |
| D7 | CLI VERSION + MCP serverInfo 둘 다 `"0.26.0"` |

### 무변경 락 (회귀 금지 확인 — 파일 미수정이면 PASS)
- relay wire / conv wire / MCP 도구 정의 / herdr RPC 시그니처 **무변경**
- 자동 close·자동 approve **없음** (hook은 힌트만)

## 산출 형식 (최종 메시지에 반드시)

```
[VERIFY-0260]
M-1: <PASS|FAIL> (M-1.1..M-1.5 요약)
M-2: <PASS|FAIL> (M-2.1..M-2.5 요약)
L-1: <PASS|FAIL>
L-2: <PASS|FAIL>
L-3: <PASS|FAIL>
D2/D4/D5/D6/D7: <PASS|FAIL 요약>
wire-lock: <PASS|FAIL>
TOTAL: <N>/12 PASS
FAILS: <없으면 none · 있으면 항목:근거 한 줄씩>
[VERIFY-0260-DONE] locks=<pass|fail> total=<n>/12
```

각 FAIL은 `file:line` 근거 필수. PASS도 핵심 1~2개 줄 번호 나열.
cwd = 리포 루트 `/Users/kyoungsiklee/projects/fable-advisor`.
