# [R-REQUEST] PLAN v0.24.0 pending-review — R37 리뷰 브리프

당신은 Loom dogfood 룸의 **claude-rev 리뷰어**다 (DOGFOOD_LOOP.md §2). 제품 코드를 구현하지 않는다.

## Scope

PLAN 0.24.0 (MINOR) — **단독 모드 기능화**: `loom relay use <name>` 명시 전환 + 프로필 relay별 신원 병존(`relays` 맵) + `loom relay local start|stop|status`. WORKFLOW §5.1 두 행 해당(신규 제품 표면 + 토큰 저장 구조 변경) → R37 필수.

## Read (순서대로)

1. `docs/PLAN.md` — **0.24.0 블록** (Changelog 최상단, `pending-review R37`)
2. `docs/plan_review.md` — R31–R36 선례(형식·기존 락 목록)
3. `docs/WORKFLOW.md` — §5(리뷰 게이트 규칙·§5.2 형식)
4. 코드 대조(읽기 전용): `packages/host/src/session-store.ts`(LoomSession :22-42 · saveSession :301-318 · sessionPath :248-261 · relayClientOptsFromSession :328-338) · `packages/host/src/relay-daemon.ts`(ensureRelay :48-140 · readRelayPid :145-153 · isRelayUp :21-35) · `packages/host/src/relay-client.ts`(:95-160, :275, :295) · `packages/protocol/src/relay-url.ts` · `packages/relay/src/room.ts`(:242-249, :278, :372-379) · `packages/relay/src/server.ts`(H-5 :81-91) · `packages/cli/src/index.ts`(relay :2956-3015 · conv-hosts 선례 :1461-1529) · `packages/host/src/conv-node-hosts.test.ts`(:24-38 테스트 격리 선례)

## 리뷰 중점 (판단 요청)

- **D8 (a)–(d) 보안 락 후보**: 토큰·시크릿 미출력 / H-6 URL·토큰 분리 / fail-closed 전환·무명 파괴 가드 / saveSession 0600 단일 경로 — 락으로 승격할지, 문안이 구현을 결정론적으로 구속하는지.
- **D2·D4 의미론**: `--as` 단독 명명 · mirror-on-save 불변식 · 무명 바인딩 파괴 가드(이종-relay create/join 에러, 동일-relay 재조인 통과)의 경계 정확성 — 특히 "동일/이종 relay" 판별 기준(relayUrl 비교)의 엣지(포트·스킴 보정·호스트 별칭).
- **D1 하위호환**: normalizeSession 무변형 통과가 기존 소비자 전 경로에 안전한지.
- **기존 불변식 유지**: H-5(비-loopback 토큰 필수)·H-6·M-7(peerSecret 0600) 침범 여부.
- **D7 out-of-scope 타당성**(allowlist relay-aware 확장 제외 근거) · U1–U3 Unknowns 처리.

## MUST (필수 절차)

1. verdict 작성 **전에** `fable-advisor` 서브에이전트를 스폰(read-only)해 자문 — 생략은 프로세스 결함.
2. `docs/plan_review.md`에 **Review R37**을 WORKFLOW §5.2 형식으로 작성(대상 버전 v0.24.0 명시).
3. Verdict: `approved` | `pending-revision` | `on-hold`. finding은 M-{n}/L-{n} 번호.

## 금지

- 제품 코드 구현·수정 (packages/ 일체)
- `docs/plan_review.md` **외** 파일 수정 (PLAN.md Status 전환도 금지 — 아키텍트 담당)
- **git commit / push 금지** — 파일 작성까지만, ship은 아키텍트 결정

## 완료 보고

pane 최종 줄에 정확히 다음 형식 한 줄을 출력:

`[R-RESULT] R37 verdict=<approved|pending-revision|on-hold> M=<개수> L=<개수> Advisor: fable-advisor consulted: yes`
