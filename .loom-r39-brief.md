# [R-REQUEST] PLAN v0.24.2 pending-review — R39 리뷰 브리프

당신은 Loom dogfood 룸의 **claude-rev 리뷰어**다 (DOGFOOD_LOOP.md §2). 제품 코드를 구현하지 않는다.

## Scope

PLAN 0.24.2 (PATCH) — **Windows 실배포 결함 2건 수정**:
- **D1** `packages/relay/src/persist.ts:389` 스냅샷 경로 가드가 POSIX `"/"` 구분자를 하드코딩 → Windows에서
  정상 파생 경로를 `Snapshot path escapes state dir`로 오탐. `path.sep` 교정. **가드 완화가 아님** — 파생
  경로가 항상 state dir 하위임을 논증 + 이탈 시도를 막는 부정 테스트를 고정.
- **D2** `packages/relay/src/server.ts:176` `handleMessage` 호출부에 try/catch 신설 — uncaught 예외가
  relay 프로세스를 죽이는 것을 차단(클라이언트 단위 fail-closed는 유지, 서버 생존).
- **D9** VERSION 0.24.1 → **0.24.2**(CLI `index.ts:144` + MCP serverInfo `stdio.ts:381` 동시).

근거: Windows 라이브 실증(err.log 스택 포집 — `Snapshot path escapes state dir` 오탐 → relay 사망 2회
재현). WORKFLOW §5.1 보안·신뢰 경계(경로 가드 계약 변경) → PATCH여도 R39 필수.

## Read (순서대로)

1. `docs/PLAN.md` — **0.24.2 블록** (`#### 0.24.2`, `pending-review R39`, 드래프트 커밋 `3263969`)
2. `docs/plan_review.md` — **R38**(직전, Plan v0.24.1 · 형식·기존 락) + R31–R37 선례
3. `docs/WORKFLOW.md` — §5(리뷰 게이트·§5.2 형식)
4. `docs/UNKNOWNS.md` — **§0.24.2** (Windows 실배포 결함 2건 unknowns)
5. 코드 대조(읽기 전용):
   - `packages/relay/src/persist.ts:376-391` — 경로 가드(D1 대상, `:389` POSIX `"/"` 하드코딩)
   - `packages/relay/src/room.ts:734-755` — 스냅샷 경로 파생부(D1 파생-경로 논증 근거)
   - `packages/relay/src/server.ts:170-180` — `handleMessage` 호출부(D2 try/catch 신설 지점)
   - `packages/relay/src/server.ts:317, 392, 485, 510` — 기존 개별 op catch(D2 중복·간섭 판정 대조점)

## 리뷰 초점 (PLAN Review impact 절 그대로 — 판단 요청)

1. **D1 파생-경로 논증 완전성**: `path.sep` 교정이 경로 가드를 **실질적으로 완화**하지 않는지 —
   스냅샷 경로가 항상 state dir 하위로 파생됨이 코드(room.ts 파생부)로 완결 논증되는지, 교정 후에도
   진짜 이탈(`..`·절대경로·심링크류) 시도가 여전히 차단되는지.
2. **D2 catch 위치**: `server.ts:176` 신설 catch가 기존 개별 op catch(`:317/:392/:485/:510`)와
   **중복 처리·간섭(이중 응답·상태 오염·에러 삼킴)**을 일으키지 않는지, 클라이언트 단위 fail-closed가
   유지되는지(서버는 살고 문제 클라이언트만 닫힘).
3. **부정 테스트 충분성**: D1 이탈-차단 부정 테스트가 회귀 가드로 충분한지(Windows `\`·POSIX `/` 양측
   구분자·`..` 이탈·절대경로 케이스 커버).

부수: D9 VERSION 이중 하드코딩(CLI+MCP) 동기 확인 · UNKNOWNS §0.24.2 처리 타당성.

## MUST (필수 절차)

1. verdict 작성 **전에** `fable-advisor` 서브에이전트를 스폰(model: fable, read-only)해 자문 — 생략은
   프로세스 결함. 전달: decision(approve vs pending-revision for PLAN v0.24.2) · constraints(WORKFLOW §5·
   경로 가드 보안 경계·non-goals) · options(PLAN/UNKNOWNS 설계 대안).
2. `docs/plan_review.md`에 **Review R39**를 WORKFLOW §5.2 형식(선례 R38 헤딩)으로 작성 — 대상 버전
   v0.24.2 명시.
3. Verdict: `approved` | `pending-revision`. finding은 M-{n}/L-{n} 번호.

## 금지

- 제품 코드 구현·수정 (packages/ 일체)
- `docs/plan_review.md` **외** 파일 수정 (PLAN.md Status 전환도 금지 — 아키텍트 담당)
- **git commit / push 금지** — plan_review.md 작성까지만, ship은 아키텍트 결정

## 완료 보고

pane 최종 줄에 정확히 다음 형식 한 줄을 출력:

`[R39-DONE] verdict=<approved|pending-revision> M=<개수> L=<개수> Advisor: fable-advisor consulted: yes`
