{
  "name": "@loom/desktop",
  "private": true,
  "version": "0.12.1",
  "description": "Loom desktop shell (Tauri 2) — sticky host UI",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  }
}
